{% set apache = salt['grains.filter_by']({
    'Debian': {
        'server': 'apache2-mpm-prefork',
        'service': 'apache2',
        'user': 'www-data',
        'group': 'www-data',
        'site_dir': '/etc/apache2/sites-enabled',
        'conf_dir': '/etc/apache2/conf.d', 
    },
}, merge=salt['pillar.get']('apache2:lookup')) %}