{% set ssh = salt['grains.filter_by']({
    'Debian': {
        'server': ['openssh-server', 'openssh-blacklist', 'openssh-blacklist-extra'],
        'server_service': 'ssh',
        'client': ['openssh-client'],
    },
    'RedHat': {
        'server': ['openssh-server'],
        'server_service': 'sshd',
        'client': ['openssh-client'],
    },
}, merge=salt['pillar.get']('ssh:lookup')) %}
